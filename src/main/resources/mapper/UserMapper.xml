<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="album.yyj.zust.aiface.mapper.UserMapper">
    <select id="findAll" resultType="album.yyj.zust.aiface.pojo.User">
        SELECT id,name,birthday from user
    </select>
</mapper>